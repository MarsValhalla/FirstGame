const numbers = [1,2,4,5,6,7,8,9,10,11,12]
//

const sq_numbers = numbers.map((number) => number * number)

console.log(sq_numbers)



// function declarations
function foo(number){
    return number * number;
}

const bar = (number) => number * number
